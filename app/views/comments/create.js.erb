function addComment(commentHTML) {
  const comment = document.querySelector('.moodboard');
  comment.insertAdjacentHTML('beforeend', commentHTML);

  $('#comment').modal('hide');
}

addComment('<%= j render "shared/projects/comment_display", project: @project, version: @version %>');
