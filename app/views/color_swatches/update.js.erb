function updateColorSwatch(colorSwatchHTML) {
  const colorSwatch = document.querySelector('.color_swatch');
  colorSwatch.innerHTML = colorSwatchHTML;
  console.log("lol")

  $('#colorSwatch, #colorSwatchEdit').modal('hide');
}

updateColorSwatch('<%= j render "shared/projects/color_swatch_ajax", project: @project, version: @version %>');
