function addColorSwatch(colorSwatchHTML) {
  const colorSwatch = document.querySelector('.moodboard');
  colorSwatch.insertAdjacentHTML('beforeend', colorSwatchHTML);

  $('#colorSwatch').modal('hide');
}

addColorSwatch('<%= j render "shared/projects/color_swatch_display", project: @project, version: @version %>');
