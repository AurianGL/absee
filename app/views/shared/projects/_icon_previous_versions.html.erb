<!-- Button trigger modal -->
<button type="button" class="btn-circle-versions" data-toggle="modal" data-target="#previousVersions">
  <i class="far fa-clone"></i>
</button>

<!-- Modal -->
<div class="modal fade" id="previousVersions" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Previous Versions</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      <div class="modal-body">
        <% if @last_version %>
          <%= simple_form_for [@last_version, @comment] do |f| %>
          <%= f.input :description %>
          <%= f.input :image %>
          <%= f.input :image_cache, as: :hidden %>
            <div class="modal-footer">
              <%= button_tag(type: 'submit', class: "btn-circle-save") do %>
              <i class="fas fa-check"></i>
              <% end %>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>
